<%= simple_form_for @restaurant do |f| %>
  <%= f.input :name %>
  <%= f.input :address %>
  <%= f.input :phone_number %>
  <%= f.input :category, collection: %w[chinese italian japanese french belgian], prompt: "Select a category" %>
  <%= f.submit 'Create Restaurant', class: "btn btn-primary" %>
<% end %>
